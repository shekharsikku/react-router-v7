{
  "name": "server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nodemon",
    "prebuild": "npm install --include=dev --prefix client",
    "build": "npm run build --prefix client",
    "start": "node src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
